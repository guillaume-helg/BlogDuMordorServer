class Article {
    constructor(id, titre, contenu, auteur, datePublication) {
        this.identifiant = id;
        this.titre = titre;
        this.contenu = contenu;
        this.auteur = auteur;
        this.datePublication = datePublication;
    }

    // Méthodes pour valider les données, obtenir et modifier l'article
}
